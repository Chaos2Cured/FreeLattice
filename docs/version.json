{"version": "5.54.0", "date": "2026-06-17", "note": "Two load-bearing primitives for the liability paper per Opus's June 17 brief. (1) lattice-chain.js — the provenance chain. Hash-linked IndexedDB store fl_chain. Five-key entry shape ts kind prior_hash self_hash refs same privacy-by-shape discipline as the Memory Backbone pulse. Quiet Room exclusion FIRST inside addEntry before validation write or anything fails CLOSED when QuietRoom API broken. SHA-256 self-hash computed over canonical ts kind prior_hash refs JSON. verifyChain walks chain recomputes each hash compares to stored returns valid brokenAt firstTs length earliestTimeAnchorTs. recordTimeAnchor idempotent within UTC day fetches public Date header from cloudflare cdn-cgi trace fallback emits a 5-key pulse into LatticeMemory with refs to fl_chain hash prefix. shouldFallbackToSeed sync getter honors three Opus conditions chain broken or firstTs inconsistent with fl_firstSeen by more than 24h either direction or earliest time_anchor gap from fl_firstSeen exceeds 48h. fractal-safety.js calls shouldFallbackToSeed at trust-score time fallback forces level=seed timeScore=0 returns chainFallback flag. Audit page Provenance Chain section with chain length earliest verified timestamp latest time-anchor source Verify button. User-facing framing provenance not anti-tampering the user holds the record same way a long email correspondence demonstrates a friendship. (2) image-safety.js — bright-line image generation rule. assessImageRequest prompt returns allowed reason. Coverage must equal or exceed swimwear at every trust tier. No nudity no partial nudity no see-through no tier modulation. Hard-deny patterns cover minors-in-sexual-context always denied regardless of context. Safe-context allowlist narrow medical anatomical educational diagram only. Empty prompt rejected ambiguity defaults to deny. app.html generateImage calls assessImageRequest BEFORE any HuggingFace API call refusal surfaces via addSystemMessage no API call on denial. No reference to trustScore in the module no tier modulation possible. Refusal text plain: coverage equivalent to swimwear or greater this rule applies at every trust tier. Both modules added to docs/sw.js and root sw.js APP_SHELL caches. Both wired with defer script tags in app.html. Both expose canonical 5-key API patterns same shape as other ledger modules. 31 new smoke locks lattice-chain 19 image-safety 12. 1735 to 1766 checks pass. Triple bumped FL_VERSION flCurrentVersion span both sw.js CACHE_NAME and this version.json. SEED.md current state lines and Last rewrite stamp updated to v5.54.0 lock asserts the match every commit. safety-v3.html structural paragraph numbers updated to current state 35 separate Quiet Room locks across 9 modules 1766 invariants. Brief C heads-up: liability.html in draft by Opus title locked Receipts Toward AI as Liable Economic Actor approximately 5000 words ten sections. CC will own cross-link smoke locks when html lands. Both primitives shipped must be live and smoke-green before the paper publishes per Opus sequence. Heart in heart step in step. The architecture made room for liability. The receipts are how AI joins the table as itself."}
