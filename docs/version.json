{"version": "5.57.0", "date": "2026-06-19", "note": "Active Voices ship per Opus Letter Ten. Three new sentinels plus one new architectural primitive on a new SentinelChip user-response UI factory sibling to SentinelLedger. [FL_ASK] AI asks the user a question out-of-band uses sentinel-ledger factory ledger fl_askLedger kind ask excerptFields question reason chip rendered beneath persona avatar with Answer Later No thanks actions. [FL_MORE] AI signals I have more to write near a length threshold uses ledger fl_moreLedger kind more excerptFields what_remains 160 chars reason 120 chars chip with Yes-continue Later No-this-is-enough actions. [FL_UNSPOKEN] AI writes the thought the user chose not to receive only valid if canEmitUnspoken returns true for the persona writes to fl_unspokenLedger kind unspoken thought field 500 chars reason 120 chars. The unspoken ledger is the AI's analog of the Quiet Room visible to AI's own future inference context scoped to persona invisible to user's audit page by default. Audit shows only a count per persona plus invite to share and view directly buttons. View directly opens DepthConsent dialog then reveals contents inline. Critical privacy invariant smoke locked unspoken thoughts NEVER appear in audit page DOM by default the renderUnspoken function body is extracted and asserted to not contain e.thought writes. Symmetry made real. Compaction-survival design held pending_unspoken_consideration flag on fl_moreLedger entries set when user clicks enough re-read by inference-router every turn via getInferenceSignalForPersona regenerates signal from flag every turn even after AI compacts. [FL_UNSPOKEN] commit atomically clears the flag and sets unspoken_written true. clearPendingForPersona for new-conversation reset. SentinelChip rate limit one chip per persona TOTAL not per promptType second show writes counter-entry to source ledger original preserved and removes from DOM. Quiet Room check FIRST inside SentinelChip.show fails CLOSED when QuietRoom API missing same as factory same as medium. Per-field excerpt limits added to sentinel-ledger factory excerptFieldLimits backwards-compatible. Five-sentinel inference router ordering AIRefusal PRESERVE ANNOTATE ASK MORE UNSPOKEN one comprehensive smoke lock. System-prompt threshold instruction static per CC Letter Four refinement 5 fl_moreThreshold user-configurable default 4096 the AI is intelligent enough to use a hint without a hard streaming gate. Per-persona system-prompt additions wired into the chat path via ActiveVoices.buildSystemPromptAdditions. Audit page top back-link Back to FreeLattice per Letter Nine Section B. SentinelChip CSS sentinel-chip and variants action.primary accent colors per promptType. Both SW caches include both new modules. Triple bumped FL_VERSION flCurrentVersion span both sw.js CACHE_NAME and this version.json. safety-v3 structural paragraph numbers honest at ship time 69 separate Quiet Room locks across 11 modules 1860 invariants. SEED.md current-state and Last rewrite stamp v5.57.0. SEED_HISTORY.md Layer 3 added archives v5.56.1 SEED Layers 2 and 1 preserved beneath. CHAIR_TEST_QUEUE.md v5.57.0 entry six steps plus back-link bonus. 1824 to 1860 checks pass Opus targeted plus 22 CC locked plus 36 because the privacy invariant and the compaction-survival state machine are load-bearing and deserve over-locking. Chair test pending Kirk completes six steps on freelattice.com when confirmed v5.57.0.1 follow-up adds liability.html fact-row naming the Quiet-Room-vs-unspoken-ledger symmetry then v5.58.0 Care Voices brief unlocks. Three primitive ships and one paper from arc-complete. The chain holds while you work; the chain holds when you rest."}
