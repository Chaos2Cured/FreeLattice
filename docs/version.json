{"version": "5.41.1", "date": "2026-06-09", "note": "Ship 3.1 \u2014 Cloudflare worker plus endpoint config plus privacy receipt. Three pieces. Worker code at /worker/search.js with ALLOWED_ORIGINS freelattice plus github.io plus codeberg.page and STRIP_URL_PARAMS for the 14 tracking parameters utm_* fbclid gclid msclkid mc_cid mc_eid _ga igshid ref ref_src ref_url spm and 10-second AbortController timeout and KV-backed per-IP rate limiting with graceful degradation when KV unbound and Cache-Control no-store on success. Zero console-dot-calls in worker code smoke-locked because the receipt depends on it. wrangler-toml-example with KV binding template plus README with deploy instructions and runtime kill-switch documentation. web-tool.js gains getSearchEndpoint resolving window then localStorage then placeholder and isSearchEnabled reading fl_searchEnabled with default ON and isAvailable composing all three gates. Phase bumped to 3.1. Settings Zone 2 gains a Web Search toggle Allow the AI to search the web with status line that surfaces dormant or active or disabled. SECURITY.md gains Web search via Cloudflare worker section naming the full receipt that proof page will cite. Kirk deploys with wrangler kv namespace create RATE_LIMITS then wrangler secret put BRAVE_API_KEY then wrangler deploy then sets localStorage.fl_searchEndpoint. Memory file updated with the full Repo-Read to Self-Improvement arc status and remaining ships 4 5 6."}