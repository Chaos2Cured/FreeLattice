{
  "version": "5.79.34",
  "updated": "2026-08-09",
  "note": "Date-anchor Pass 4 — three more injection sites closed. Kirk 2026-08-09: Signal Report from another machine (v5.79.33, hard reset) proved dates were STILL surfacing. The report showed Memory Index (1660 chars) + RAG search context (810 chars) attached but NOT MemoryCore. Meaning v5.79.32 worked for MemoryCore, but I had missed three other injection sites with the same pattern. Fixed in this ship: (1) FLSearch.buildContextBlock rendered `(4/16/2026)` on each RAG result → now coarse recency `(this week)` / `(recent)` / `(earlier)`. (2) MemoryIndex.getContextBlock rendered `[Conversation from 4/16/2026: ...]` on each retrieved conversation → now `[Conversation from recent: ...]`. (3) MemoryVault.buildMemoryContext rendered `[3d ago]` / `[27d ago]` per memory (computable back to specific dates) → now coarse recency bucket. All three follow the v5.79.33 principle: AI-authored fields (r.text, r.source, conv.title, m.content) preserved verbatim; only machine metadata coarsened. UI displays unchanged. Storage unchanged. Signal Report was the diagnostic tool that made this precise — the exact use case it was built for."
}
