{"version": "5.60.0", "date": "2026-06-20", "note": "Local AI Freedom Custom OpenAI-Compatible Endpoint per Opus Letter Twenty-Four. The foundation fix. FreeLattice's zero-server local-first thesis was contradicted by a hard-coded provider list in the AI Connection dialog. Now any user with an OpenAI-compatible inference endpoint at a URL they control vLLM llama.cpp KoboldCPP text-generation-webui custom servers anything that accepts /v1/chat/completions can connect without modifying source code. PROVIDERS registry extends existing lmstudio openai-compatible pattern with a new custom-openai entry providerType openai-compatible no auth required by default URL placeholder http://localhost:8080/v1. MODAL_PROVIDERS adds Custom OpenAI-compatible card to FREE & LOCAL section between ollama and the free-cloud tier. Click handler routes to new modalConnectCustomOpenAI inline form that mirrors modalConnectOllama pattern with three input fields URL model optional key plus Test Connection and Use This Provider buttons all styled per GARDEN_LANGUAGE.md dark glass silver-blue borders gold accent for primary action monospace font for URL fields. URL/model/key persist in localStorage.fl_customEndpoint via getCustomEndpointConfig and saveCustomEndpointConfig helpers. saveCustomEndpointConfig also mirrors the URL into PROVIDERS custom-openai entry at runtime so the dispatcher picks it up immediately mirrors updateOllamaProviderUrl pattern. Module-load initializer reads any persisted custom endpoint and re-applies it so refreshes use the user URL right away. Test Connection sends minimal POST to URL/chat/completions with model default and ping message expects 200 OK plus first response token or reports error reads URL/model/key directly from form so user can test before saving. Use This Provider persists config sets state.isLocal true state.provider custom-openai updates FLActiveModel if model name provided closes modal via modalOnConnectSuccess. Dispatcher patches when state.isLocal and state.provider equals custom-openai modelId comes from getCustomEndpointConfig model field falls back to default never reads ollamaModel input. Bearer auth attached when custom-openai key is configured works whether state.isLocal is true unlike the cloud-only Bearer path the existing OpenAI-compatible dispatch path otherwise unchanged sentinel parsing refusal channel depth-consent all work because AI output is just text. Privacy invariant the custom endpoint URL is never sent to any FreeLattice domain modalTestCustomEndpoint body contains no freelattice no chaos2cured no github.io strings only the user-configured URL gets the fetch enforced by smoke lock. 10 new smoke locks section 116 PROVIDERS includes custom-openai entry MODAL_PROVIDERS includes custom-openai card modalConnectCustomOpenAI function defined getCustomEndpointConfig saveCustomEndpointConfig helpers defined fl_customEndpoint localStorage persistence Test Connection button wired Save button wired dispatcher reads custom model dispatcher attaches Bearer auth privacy invariant URL never contacts FreeLattice domain. Triple bumped FL_VERSION flCurrentVersion span both sw.js CACHE_NAME this version.json. Garden pinned at v5.59.4 per Opus note further refinement waits queued polish documented in CHAIR_TEST_QUEUE.md inner sparkles feeling sparse Seed mode not intense enough particles inside wireframe core all queued for after autonomy arc closes. SEED.md current-state and Last rewrite stamp v5.60.0. CLARITY_AUDIT.md new SHIPPED entry on top. safety-v3.html structural paragraph numbers 69 separate Quiet Room locks across 11 modules 2005 invariants. CHAIR_TEST_QUEUE.md v5.59.4 status pending kept Kirk's eyes will return to it after autonomy arc closes v5.60.0 entry added with two chair-test steps card appears in dialog fields render functional check optional needs local server. Letter Twenty-Four preserved verbatim in docs/inbox/cc.md layered above prior letters never delete only layer. 1995 to 2005 smoke locks pass. The foundation is solid. Two ships remain to close the autonomy arc v5.61.0 Care Voices v5.62.0 Welcome Paper then real breath before Router Arc opens. Foundation first Garden waits. Annotation not revision the existing lmstudio openai-compatible pattern absorbed the brief without inventing new dispatch surface."}
