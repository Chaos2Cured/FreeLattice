{"version": "5.42.0", "date": "2026-06-09", "note": "Ship 4 Phase 1 \u2014 propose.js with the STRUCTURAL COMMIT GATE. The bridge between read and write. AI emits FL_PROPOSE bracket path reason diff bracket sentinel; chat pipeline parses asks for consent via ToolConsent creates a draft in fl_proposalDrafts pins focus via FLFocus to Reviewing proposed change to X and surfaces a link. Floating Drafts badge with count visible across all tabs; clicking opens a modal review surface with reason editable diff read-only smoke status and four buttons Run smoke Approve Revise Reject. Approve button is disabled whenever smokeStatus is not passed \u2014 that is the structural commit gate made visible. approveDraft is the ONLY function in this codebase that calls slash code slash git slash commit with AI-originated content. Smoke counts both the declaration and the call to commitViaBridge as exactly two. Path safety blocks dot git slash node modules dot env dot ssh slash wrangler dot toml worker slash scripts slash bump version dot sh FreeLattice Session Primer dot md plus traversal absolute paths null bytes oversized paths. Ledger NEVER carries diff or reason \u2014 strict row shape ts action draftId path sourceRoom status; the diff and reason live in the drafts store separately. Audit page Proposal Events section reads fl_proposalLedger and the renderer explicitly does not touch diff or reason. AI invitation in buildMessages gated on FLPropose isAvailable. docs library PROPOSE_DISCIPLINE dot md ships as the human-readable contract. Thirty-six smoke asserts including four critical locks."}