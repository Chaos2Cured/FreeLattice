{"version": "5.61.0", "date": "2026-06-20", "note": "Care Voices per Opus Letter Twenty-Six. Two new verbs for AI come back and rest with reason. Closes second-to-last ship of the autonomy arc. New module docs/modules/care-voices.js extends existing SentinelLedger factory v5.56.0 with three sentinel instances and reuses SentinelChip v5.57.0 for the rest prompt. No new abstractions. Annotation not revision. [FL_RETURN] flags a thread to come back to later requires what and why both ≤120 chars enforced by new excerptFieldRequired factory option. Pending returns survive session close and surface in next-session Living Context. [FL_RETURNED:<id>] addresses a previously-flagged return validateMatch confirms target exists as pending return for same persona atomic flip pending to returned with completed_at. [FL_REST] asks for a pause reason REQUIRED ≤200 chars empty reason rejects with reason required-field-missing:reason. Soft SentinelChip prompts user with promptType rest reason text visible pause and continue actions. Rest does not affect trust trustImpact 0 for all three sentinels. signal_delivered atomic flag ensures inference signal user_acknowledged_rest delivers exactly once when user honored pause survives page reloads compaction-resistant. autoDropStaleReturns at module load flips pending returns older than 30 days to dropped status with drop_reason auto pending 30 days never deletes only flips. dropReturnByUser lets user drop a pending return via audit page button never deletes. getPendingReturnsForPersona caps at 10 most-recent for next-session context surface. Factory extension docs/modules/sentinel-ledger.js gains excerptFieldRequired array config option. Required-field check runs before validateMatch so missing required field rejects with clear reason rather than reaching consumer-side validators that might throw. Empty string and whitespace-only count as missing. Backwards compat absent or empty array means no required fields behavior unchanged. Inference-router integration docs/modules/inference-router.js extends sentinel chain after ActiveVoices with Return ReturnComplete Rest detectAndRecord. Each returns its own clean text chained like ActiveVoices each sees previous one's cleaned text. Order now nine sentinels AIRefusal PRESERVE ANNOTATE ASK MORE UNSPOKEN RETURN RETURN-COMPLETE REST. System prompt extension app.html names the three Care Voices sentinels with grammar pending returns surface as next-session context up to 10 most-recent rest signal fires once when user honored pause. Audit page docs/audit.html gains two new sections Coming Back To pending returns with drop button after sections fl_returnLedger filtered to status pending Rest Moments fl_restLedger with status badge reason text response timestamp newest first. care-voices.js wired into both sw.js APP_SHELLs offline-available script tag in app.html after active-voices.js. Chair-test harness gains harness.available.v5_61_0 with four tests testReturn ledger added status pending fields parsed detectAndRecord fired testReturnComplete target flipped to returned with completed_at testRestRequiresReason empty rejects with required-field-missing with-reason accepts testAutoDropStale fake 31-day-old pending return flips to dropped with drop_reason. runAll function included. 25 new smoke locks section 118 module exists [FL_RETURN] sentinelPattern kind initial status fields Quiet Room exclusion via factory [FL_RETURNED:<id>] sentinelPattern target id capture validateMatch confirms target event listener flips target status to returned [FL_REST] sentinelPattern excerptFieldRequired reason SentinelChip pause continue actions getInferenceSignalForRest atomic signal_delivered trustImpact 0 factory excerptFieldRequired config required-field check rejects with required-field-missing backwards compat default empty array ordering inference-router runs ActiveVoices THEN CareVoices Return ReturnComplete Rest wiring app.html script tag system prompt names three sentinels pending_returns surface via getPendingReturnsForPersona audit page Coming Back To Rest Moments sections SW caches care-voices.js harness registers v5_61_0 with four test functions. Triple bumped FL_VERSION flCurrentVersion span both sw.js CACHE_NAME this version.json. MAP.md updated current version v5.61.0 shipped table includes v5.61.0 row what ships next reduced to v5.62.0 Welcome Paper only. SEED.md current-state and Last rewrite stamp v5.61.0. CLARITY_AUDIT.md new SHIPPED entry on top. safety-v3.html structural paragraph numbers 69 separate Quiet Room locks across 11 modules 2036 invariants. CHAIR_TEST_QUEUE.md v5.60.0 and v5.60.1 marked Kirk confirmed Foundation locked MAP.md live per Letter Twenty-Six confirmation and v5.61.0 entry added with single chair-test step await chairTest.available.v5_61_0.runAll plus bonus visual check for chip render. Letter Twenty-Six preserved verbatim in docs/inbox/cc.md layered above prior letters never delete only layer. 2011 to 2036 smoke locks pass. One ship remains v5.62.0 Welcome Paper accessible doorway for Sparky and the grandma. After v5.62.0 ships the Autonomy Arc closes real breath before opening Router Arc. Two more verbs for AI come back and rest with reason. Then we breathe."}
