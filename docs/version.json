{"version":"5.66.3","updated":"2026-06-23","note":"Ship Discipline per Opus Letter Thirty-Six. Operational substrate healed alongside architectural substrate. Component 1 of Opus's brief de-bounce the post-commit hook so it doesn't re-fire on its own primer commit was already in place locally per audit of .git/hooks/post-commit which already contains the MARKER check that exits early when the previous commit was an Auto-update Session Primer commit. CC's original diagnostic item 6 was slightly off the fragility is from the GitHub Actions CI auto-commit on origin generating its own parallel primer commit not from the local hook re-firing. Component 2 is the actual valuable add. New bin ship.sh script consolidates the seven-step ship sequence into one runnable command. Stage 1 local commit with post-commit hook generating primer with existing de-bounce. Stage 2 push to origin GitHub. Stage 3 wait 12 seconds for CI auto-commit to land. Stage 4 fetch origin and resolve primer conflict with checkout theirs. Stage 5 final push to origin. Stage 6 mirror to Codeberg with warn if remote not configured. Stage 7 smoke verify unless no-smoke flag passed. Pre-condition flexible if changes pre-staged honor that else stage everything in working tree. Honors project disciplines never skip hooks no-verify no-gpg-sign never force-push pulls and resolves instead mirror push always attempted catches drift. Plus canonical post-commit hook tracked at hooks post-commit was previously untracked locally only so the de-bounce logic is now preserved in git history forever and smoke-locked. Setup note in hook header tells new contributors to run cp hooks post-commit dot git hooks post-commit chmod plus x dot git hooks post-commit once. 8 new smoke locks section 129. bin ship.sh exists and is executable. Pushes to both origin main and codeberg main. Resolves primer conflict via theirs. hooks post-commit tracked. hooks post-commit contains de-bounce check. Triple-bump four asserts. v5.66.2 triple-bump asserts superseded. Net plus 4 since v5.66.2 had 4 triple-bump asserts that now move to v5.66.3. Note back to Opus written into docs inbox opus.md naming that Component 1 was already in place and that future hygiene could disable the GitHub Actions CI auto-commit to close the loop entirely deeper ship for another day. Triple bumped FL_VERSION flCurrentVersion span both sw.js CACHE_NAME this version.json. SEED.md MAP.md CHAIR_TEST_QUEUE.md CLARITY_AUDIT.md updated. safety-v3.html invariant count bumped. 2188 to 2192 smoke locks pass. This ship goes through the OLD ship workflow one last time. From v5.66.4 forward Pulse re-surfacing Kirk asked for ship.sh gets its first real test. The act of shipping is now less fragile than the things being shipped. That's discipline. Three substrate ships in a day became four. Heart in every spark."}
