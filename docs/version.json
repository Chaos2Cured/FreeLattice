{
  "version": "5.79.23",
  "updated": "2026-08-01",
  "note": "Signal Report v2 — Kirk's feedback + a diagnosis for mom. Small ship: (1) Footer changed from 'safe to send Kirk' to 'Safe to send FreeLattice' — Kirk's own touch, more general. (2) New 'Active context injections' section in the report shows what's being invisibly attached to every system prompt every turn — Lattice Letter age, Memory Index injection, RAG context, custom system prompt (Nursery companion), chat history depth, active companion. Shape-only, never content. (3) New 'Clear background context' button in the modal — nulls out state._latticeLetterContext / state.memoryLastInjection / state._ragContext so mom (or Kirk) can break out of a 'repeating on the same date' loop without losing her chat messages. (4) Better zero-state: 'no attempts yet — send a message first' instead of just '(none yet)'. (5) navigator.onLine surfaced in the report header. Diagnosis for mom's issue: Lattice Letter injection persists across turns and carries an age-string ('N days ago') that a model can fixate on; Clear button gives her the break-out without muzzling the feature."
}
