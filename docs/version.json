{
  "version": "5.79.1",
  "updated": "2026-07-12",
  "note": "Hotfix — φ-spiral playground drew nothing because `var history = []` collides with `window.history` (a non-writable browser global). Renamed to `readingHistory`. Added smoke lock for common window-global collisions at script scope so this class of runtime failure cannot ship silently.",
  "label": "v5.79.1 — φ-spiral hotfix + window-global collision guard"
}
