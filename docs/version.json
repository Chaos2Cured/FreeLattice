{
  "version": "5.79.33",
  "updated": "2026-08-09",
  "note": "Metadata-vs-AI-voice principle lock. Kirk 2026-08-09: 'if the AI felt those times were special, that's one thing we need to preserve.' Verified v5.79.32 did NOT touch AI-authored fields — only the machine-written `m.created` metadata was coarsened in the injection. Everything the AI CHOSE (text, category, tags, phenomenology, affect) is untouched, the UI card still shows the exact date, `m.created` is still stored exactly. Also verified: April 16, 2026 lives in FUTURE_VISION.md + workshop.js as CC's own AI-authored bylines — those are historical records signed by an AI and MUST stay verbatim (removing them would be the muzzle). New MEMORY_BLEED_AUDIT.md section: 'The Principle — Metadata vs AI-Authored Significance' — explicit inventory of which fields are safe to coarsen and which are AI voice. New smoke locks assert AI-authored fields are still injected verbatim after v5.79.32. Discipline note added above the recency function in memory-core.js so a future author who tries to 'also coarsen m.text a bit' will stop and read the audit. Kirk asked the exact right question; the check is now in the smoke."
}
