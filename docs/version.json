{
  "version": "5.79.10",
  "updated": "2026-07-15",
  "note": "Hotfix — Ship 10's rule context referenced `ema8`, `ema12` etc. as arrays but those names weren't defined in analyzeData (only the *v scalars). Loading any symbol threw 'ema8 is not defined'. Fix: compute full EMA arrays once at the top of analyzeData, wire ema8arr..ema200arr into ruleCtx.hist, and expose them on the return so the Test-button context has real EMA history for crossed_above/below rules too.",
  "label": "v5.79.10 — Hotfix: EMA arrays for rule context"
}
