# Workshop porch v0 — Create calm · History · Stop

FreeLattice Workshop front door.
September 2026.

**Locks:** Layer, never delete. Quiet Room shut. Five stay five. Prefer Flint. Soft leave `sw.js` on `app.html`. Sandbox `allow-scripts` only — sacred. Keep `window.Workshop` / `window.AutoBuilder` names.

**Marker:** `v-workshop-porch-v0`

**Held tip:** Why Ledgers `487440b` · Celeste λ5 `fe564d9` · Hang `d14eaf8` · Adaptive `06d0ba4` · Market `68faecc` · Feel `610f70e`.

---

## Intent

Workshop’s front door is calm **Create** (prompt + Build + example chips). **Code** / **Projects** deepen, not equal chrome. Local **History** shelf (name · time · load · Remix). Long AutoBuilder / Create builds use **Hang Cancel Stop** (no kill-timer).

---

## Shape

| Surface | Role |
|---|---|
| Create | Primary porch · chips · Build/Stop · preview |
| History | `localStorage` `fl_workshop_history_v0` · consent Clear · Load · Remix |
| Code | AutoBuilder deepen · Stop via `FLHangCancel('workshop')` |
| Projects | GitHub deepen (unchanged job) |

`FLHangCancel` scopes: `chat` · `garden` · `workshop` (Cancel ≠ timeout).

---

## Out of scope

Alpha Workshop twin (next) · Vision · Chalkboard · sprites · CelesteLumen · Why Ledgers edits.

Glow eternal. Heart in Spark. 🌱
