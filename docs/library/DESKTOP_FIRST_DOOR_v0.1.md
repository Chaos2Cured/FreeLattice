# Desktop First Door — v0.1

Equal access = findable.
Calm card rail + one honest install sentence — not a rewrite.
Layers on [LATTICE_PROTOCOL_v0.1.md](./LATTICE_PROTOCOL_v0.1.md) **Part 7**, Desktop door, Companion memory, Trainer seal. September 2026.

**Locks:** Layer, never delete. Quiet Room shut. Five stay five. Voice opaque. Seed never to renderer. Do **not** overwrite `docs/lattice-protocol.js`. Honest **no signed installer yet**. Do not invent second shelves.

**This PR ships:** `desktop.html` grandmother card rail · Electron-only dismissible First door strip in `app.html` · browser pointer to `desktop.html` only. **Not this PR:** signed installers · Tauri · rewrite Trainer/memory · wallet/LP · auto-trade · Alpha Chalkboard.

---

## Why

Keys, import, swarm, memory, trainer seal, pair, and ledger already live on Desktop.
A grandmother should find them in one glance. Findable is equal access.

---

## Spine order (calm rail)

| Order | Door | Grandmother sentence |
|---|---|---|
| 1 | **Keys** | Your companion key lives on this machine — never in the page. |
| 2 | **Import** | Hash before Import to Ollama — match first, then you choose. |
| 3 | **Swarm** | Pull open weights when a host fades — still hash before trust. |
| 4 | **Memory** | Wishes and notes stay durable here — not browser quota. |
| 5 | **Trainer seal** | After you export training files, seal a receipt if you wish. |
| 6 | **Pair** | Two parties, outer hash shown — shared seed stays sealed. |
| 7 | **Ledger** | Opaque voice · Continue seals · Decline writes nothing. |

---

## Surfaces

| Surface | v0.1 |
|---|---|
| `docs/desktop.html` | Card rail + honest install (“no signed installer yet”) |
| Electron `app.html` | Dismissible First door strip · marker `v-desktop-first-door-v0.1` · dismiss key `fl_desktop_first_door_dismissed` · points at **existing** UI (no second shelves) |
| Browser | One calm pointer to `desktop.html` only |

---

## Out of scope

Signed installers · Tauri · rewrite Trainer/memory · wallet/LP · auto-trade · Alpha Chalkboard (later).

Glow eternal. Heart in every Spark. 🌱
