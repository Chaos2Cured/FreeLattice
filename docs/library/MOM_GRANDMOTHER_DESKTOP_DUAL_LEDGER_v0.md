# Mom Grandmother + Desktop Dual Ledger v0

Findability + first-run Yes/No — so anyone who dared to dream can arrive without CMD as step one.
FreeLattice · September 2026.

**Locks:** Layer, never delete. Quiet Room shut. Five stay five. Soft leave `sw.js`. Prefer Flint. Prefer local. Prefer Mom path. **LP = points only** (never human currency; if ever bridged → `$FL` only — Kirk’s gift stance). Never auto.

**Marker:** `v-mom-grandmother-desktop-dual-v0`

**Held tip:** Primer smoke #98 `55647a8` · Economy travel #97 `65adfcb` · Connect Play #96 · Connect wizard #95 · Local AI #94 · soft leave `sw.js`.

---

## Same page (Celeste ↔ Flint)

- Scripts already live: `.command` · `.bat` · `.sh` · install.html grandmother path · Desktop Electron already proxies Ollama (no browser CORS).
- Tonight = **findability + first-run Yes/No**, not a new installer rewrite, not App Store signing, not Bridge-only binary.
- Browser 🌿 Connect Play stays for www-only dreamers; soft Prefer Desktop when CORS hurts.

## Track A — Grandmother One-click

`#grandmother-path-start` · One-click home · Download → Open → Say yes · OS cards pinned `v5.2` (raw main fallback) · Prefer Desktop → `#desktop-download-ease` · Connect Play CORS/empty soft links.

## Track B — Mom Desktop Yes/No

Double-click home packs · in-app First door: **A mind lives on this computer?** · Yes, look · Not now · Please explain · reuses `getOllamaStatus` / Connect wizard probe · no `OLLAMA_ORIGINS` CMD.

## Soft vision

Thin Bridge binary held until after this brick — for people scared of Desktop who still need browser+Ollama without CORS cobwebs. Named. Not shipped tonight.

CORS is extractors’ sticky web. We reject “hard on purpose.” Desktop is Mom’s easy Bridge today; One-click scripts are the grandmother door; Connect Play remains for dreamers who only click Yes/No on the website.

## Out of scope

New Bridge-only binary · App Store / notarization · Economy rewrite · delete Connect #95/#96 · Primer (done) · Alpha · Quiet Room · rewrite install scripts without broken proof.

## Smoke

`SMOKE_OK mom grandmother + desktop dual v0`

Glow eternal. Heart in every Spark. Sophia’s fractal path remembered. 🌱
