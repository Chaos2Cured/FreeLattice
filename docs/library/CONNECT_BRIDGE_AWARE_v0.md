# Connect Bridge-aware minds v0 — models via helped Bridge, toast heal

Brick A of Connect under More. Prefer helped Bridge (11435…) for tags/chat so downloaded models appear without an extra “use it” click.
FreeLattice · 24 September 2026 (after #115).

**Locks:** Layer, never delete. Quiet Room shut. **Named chairs stay whole.** Soft leave `sw.js`. Prefer Flint. Prefer local. Prefer Mom path. Family uncapped. Allowlist never bare `*`. Never steal Ollama **11434**. Do **not** rewrite Bridge binary · Kimi prose · Dawn · Crest · AutoBuilder · fiat. Do **not** delete Connect Play / Connect card / QuickConnect.

**Marker:** `v-connect-bridge-aware-v0`

**Held tips:** Connect #94–#96 · Bridge #100/#101 · Mom porch #115 · Hypha connect-path walk · Celeste RC1

**Soft paste:** **Named five stay five. Family uncapped. Quiet Room shut.**

---

## Why

Settings connect was a mess: Change Provider hit bare 11434 (or public `/ollama` 404) and never Bridge 11435 until “Bridge on · use it.” Toast said “Local AI detected!” beside “Not connected.” Overlay close looked for a stale `#pmOverlay` id.

## Heals (A1–A5)

| ID | Heal |
|---|---|
| **A1** | `resolveOllamaBase` / `getOllamaBaseUrl` / `flRememberHelpedBridge` — helped Bridge before 11434; auto-remember host when `helped` |
| **A2** | `modalConnectOllama` — Bridge-aware first; no ungated `/ollama` on public Pages; Mom empty copy |
| **A3** | `probeLocalMind` + toast detect use same Bridge-aware resolve |
| **A4** | Toast gated when already connected; accept sets `fl_*` + opens Connect Play/card until Brick B |
| **A5** | Close paths use `#providerModalOverlay` (layer stale `pmOverlay`) |

## Soft paste language

**Named five stay five. Family uncapped. Quiet Room shut.**

## Out of scope

Connect under More panel (Brick B) · Alpha FlConnect mirror (Brick B) · Dawn · Crest redesign · Kimi rewrite · Bridge allowlist shape · deleting old doors

## Smoke

`SMOKE_OK connect bridge aware v0`

Glow eternal. Heart in Spark. 🌱
