# Bridge Win/Linux · Port channel 11435 · Friction heal · Ledger v0

Equal-access morning brick — artifacts for every OS · reserved Bridge channel · CMD last.
FreeLattice · September 2026.

**Locks:** Layer, never delete. Quiet Room shut. Five stay five. Soft leave `sw.js`. Prefer Flint. Prefer local. Prefer Mom path. **LP = points only.** Never auto. **Never bare `Access-Control-Allow-Origin: *`.** Never steal Ollama **11434**.

**Marker:** `v-bridge-win-linux-port-friction-v0`

**Held tip:** Bridge #100 `0bde7e9` · Mom dual #99 · Primer #98 · Economy #97 · Connect Play #96/#95.

---

## Why never `*`

If Bridge answered CORS with bare `*`, any website could ride into local Ollama. Allowlist = love-as-safety for Mom and Jeffrey. Self-hosters may **add their origin** — choosing a channel of trust — never open the whole web.

## Port channel

| Service | Default |
|---|---|
| Ollama | **11434** (never steal) |
| FreeLattice Bridge | **11435** |

`--port` · `FREELATTICE_BRIDGE_PORT` · config · clash → try next / choose. Connect Play follows live port. Soft: “Bridge on another channel?”

## Friction heal

Connect Play CORS / empty lead with:
1. Bridge · 2. Desktop · 3. One-click · 4. Advanced Terminal/`OLLAMA_ORIGINS` (collapse)

## Artifacts

Release `bridge-v0.1` (or `.1.1`): Mac zip · Windows Portable + Setup · Linux AppImage — names match `install.html#bridge-download`.

## Smoke

`SMOKE_OK bridge win linux port friction v0`

Glow eternal. Flow eternal. Heart in Spark. 🌱
